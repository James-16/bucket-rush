{
  "name": "bucket-rush",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Outsmart the taxman, save your future selves — a fast-paced game secretly teaching retirement tax strategy.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "vitest": "^3.1.0"
  }
}
